[browser]
gatherUsageStats = false

[server]
headless = true

[theme]
primaryColor = "#2A9D8F"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F6F8FB"
textColor = "#1F2937"
font = "sans serif"

# Page title and wide layout are set in app.py via st.set_page_config,
# because Streamlit does not expose those options through config.toml.
